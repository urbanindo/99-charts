apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "base-demo.fullname" . }}-test-connection"
  labels:
    {{- include "base-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "base-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
