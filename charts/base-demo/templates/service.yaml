apiVersion: v1
kind: Service
metadata:
  name: {{ include "base-demo.fullname" . }}
  labels:
    {{- include "base-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "base-demo.selectorLabels" . | nindent 4 }}
